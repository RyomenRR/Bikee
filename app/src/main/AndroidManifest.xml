<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ryomen.bikee">

    <!-- 📡 Bluetooth & Location permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- 📱 Android 12+ Bluetooth permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" android:required="false" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:required="false" />

    <!-- 🎤 Voice & Internet -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bikee">

        <!-- 🏍️ Main Activity (Handles launcher, shortcut & assistant trigger) -->
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:exported="true">

            <!-- 📱 Launcher icon -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- 🔗 Shortcut or Assistant trigger (bikee://start_bike) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="bikee" />
            </intent-filter>
        </activity>

        <!-- 🎯 Receiver for pinned shortcut broadcast -->
        <receiver android:name=".StartBikeReceiver" android:exported="true">
            <intent-filter>
                <action android:name="com.ryomen.bikee.START_BIKE" />
            </intent-filter>
        </receiver>
    <service android:name=".BluetoothService" android:exported="false" />
        <!-- 🛠 Voice Command Editor -->
        <activity
            android:name=".CommandEditActivity"
            android:exported="true" />

        <!-- 🧠 Invisible Shortcut Handler -->
        <activity
            android:name=".StartBikeActivity"
            android:exported="true"
            android:theme="@style/Theme.Bikee.ShortcutNoUI" />

        <!-- 🚀 Shortcut XML config (for Android launcher) -->
        <meta-data
            android:name="android.app.shortcuts"
            android:resource="@xml/shortcuts" />
    </application>
</manifest>